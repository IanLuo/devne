metadata:
  name: test_project
  version: 0.0.1 
  description: description for test project

units:
  db.postgres:
    username: "ss_db"
    password: "admin"
    database: "password"

  language.python:
    version: 310 
    libs:
      default:
        - typer 
        - black
      development:
    test: python.pytest

tools:
  - black
  - django

# this is the official nixpkgs ref
nixpkgsrev: xxx
